name: Deploy Setup
# This file is a placeholder to resolve stale IDE errors.
# Please rely on ci.yml for actual workflows.
on:
  workflow_dispatch:

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder"
